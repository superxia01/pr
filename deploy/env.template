# 后端环境变量模板
# 生产环境: /var/www/pr-business/.env
# 开发环境: backend/.env

APP_ENV=production
APP_PORT=8080

# 数据库配置（通过 SSH 隧道连接杭州数据库）
DB_HOST=localhost
DB_PORT=5432
DB_USER=nexus_user
DB_PASSWORD=hRJ9NSJApfeyFDraaDgkYowY
DB_NAME=pr_business_db

# JWT 密钥（生产环境必须修改）
JWT_SECRET=CHANGE_THIS_TO_SECURE_RANDOM_STRING

# Auth Center 认证服务
AUTH_CENTER_URL=https://auth.crazyaigc.com

# 日志级别
LOG_LEVEL=info
