# ============================================
# PR Business 环境变量配置示例
# ============================================
# 说明：这是模板文件，提交到Git仓库
# 实际部署时，服务器上手动创建 .env 文件，不要提交到Git

# ============================================
# 应用配置
# ============================================
APP_ENV=production
APP_PORT=8081
APP_NAME="PR Business"
APP_DEBUG=false

# ============================================
# 数据库配置（通过 SSH 隧道）
# ============================================
# ⚠️ 重要配置说明：
# 1. DB_HOST 必须是 localhost（通过 SSH 隧道转发到杭州服务器）
# 2. DB_USER 所有系统统一使用 nexus_user
# 3. DB_PASSWORD 统一使用 hRJ9NSJApfeyFDraaDgkYowY
# 4. DB_SSLMODE=disable 因为 SSH 隧道已加密
DB_HOST=localhost
DB_PORT=5432
DB_USER=nexus_user
DB_PASSWORD=hRJ9NSJApfeyFDraaDgkYowY
DB_NAME=pr_business_db
DB_SSLMODE=disable
DB_MAX_OPEN_CONNS=100
DB_MAX_IDLE_CONNS=10
DB_CONN_MAX_LIFETIME=3600

# ============================================
# Auth Center配置
# ============================================
AUTH_CENTER_URL=https://os.crazyaigc.com
AUTH_CENTER_REDIRECT_URI=https://pr.crazyaigc.com/api/v1/auth/callback

# ============================================
# 前端配置
# ============================================
FRONTEND_URL=https://pr.crazyaigc.com

# ============================================
# JWT配置
# ============================================
# ⚠️ 生产环境必须使用强随机密钥
# 生成命令: openssl rand -base64 32
JWT_SECRET=change-this-to-a-strong-random-string-in-production
JWT_ACCESS_TOKEN_EXPIRE=24h
JWT_REFRESH_TOKEN_EXPIRE=168h  # 7天

# ============================================
# 文件存储配置
# ============================================
UPLOAD_MAX_SIZE=5242880  # 5MB
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/gif

# ============================================
# 日志配置
# ============================================
LOG_LEVEL=info
LOG_FORMAT=json
LOG_OUTPUT=stdout
